<h1>Editing Answer</h1>

<p><%= @answer.question.title %></p>

<%= render 'form', answer: @answer %>

<%= link_to 'Show', @answer, class: 'btn btn-outline-dark' %> |
<%= link_to 'Back', admin_question_path(@answer.question), class: 'btn btn-outline-dark' %>
