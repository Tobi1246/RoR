<table>
  <tr>
    <th><%= question.title %></th>
    <th><%= link_to t('.show'), admin_question_path(question), class: "btn btn-outline-info", method: :get %></th>
    <th><%= link_to t('.edit'), edit_admin_question_url(question), class: "btn btn-outline-warning", method: :get%></th>
  </tr>
</table>
